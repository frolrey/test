<?php
/*Дан некоторый массив, например, вот такой:

[1, 2, 3, 4, 5, 6]
Поделите сумму элементов, стоящих на четных позициях, на сумму элементов, стоящих на нечетных позициях.*/
$z = range(1,6);
$h = [];
$j = [];
$count = 0;
for ($i = 0; $i<count($z); $i++)
{
	if ($i % 2 == 0)
	{
        $h[] = $z[$i];
	}
	else {
        $j[] = $z[$i];
	}
}
echo "<pre>";
echo (array_sum($h) / array_sum($j));